# Discord Bot Configuration
DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here
GUILD_ID=your_guild_id_here

# Bot Configuration
CLEANUP_INTERVAL=0 */6 * * *

# Optional: Set scrim channel via environment (can also be set via /setup command)
# SCRIM_CHANNEL_ID=your_scrim_channel_id_here

# Note: Admin permissions are now handled via Discord Administrator role
# No need to set ADMIN_IDS - anyone with Administrator permissions can use admin commands